import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#827717",
      secondary: "#5b5310"
    },
    secondary: {
      main: "#9b9245"
    }
  }
});

export default theme;
